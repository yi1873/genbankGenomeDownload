[package]
name = "genbankGenomeDownload"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.12", features = ["blocking", "rustls-tls"], default-features = false }
clap = { version = "4", features = ["derive"] }
flate2 = "1"
regex = "1"
indicatif = "0.17"
